# gateway stdout/stderr captured by restart.sh
gateway.log

# Python virtualenv + caches for the agent/ chat client
agent/.venv/
__pycache__/
*.pyc
.env
# post deployment variables installed by provisioner
post_deploy_variables.json
# IBM Verify tenant client secret for the Verify IdP path
.env.verify

# Generated from policy-verify-opa.yaml.tmpl by render-verify-config.sh. The
# tenant URL and exchange client_id are tenant-specific, and Verify generates
# client ids, so the rendered file is per-tenant rather than shared. Edit the
# .tmpl. NOTE: this file was tracked before the template existed — the ignore
# only takes effect after `git rm --cached policy-verify-opa.yaml`.
policy-verify-opa.yaml
